[project]
name = "deepagents-poc"
version = "0.1.0"
description = "Deep Agents POC - Tax QA Agent"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.6.3",
    "python-docx>=1.1.2",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
